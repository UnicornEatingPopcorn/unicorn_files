const Crittr = require('crittr');

Crittr({
    urls: ['http://127.0.0.1:8080/header.html'],
    css: './style.scss',
    device: {
        width: 1920,
        height: 1080,
    },
})
    .then(({ critical, rest }) => {
        console.log(critical);
    })
    .catch(err => {
        console.error(err);
    });
